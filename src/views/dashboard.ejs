<%- include('partials/header')%>
    <h1>Dashboard</h1>
    <p>Welcome back again, <%= user.username %>, These are you servers:</p>

    <div>
        <% user.guilds.forEach((guild) => { %>
            <%- include('partials/guild', {guild})%>
        <% }) %>
    </div>

<%- include('partials/footer')%>